# Core -- Solution 1 (DriftFind, classical) and the dataset generator.
# CPU-only, no GPU needed. These three alone run the classical inference and
# regenerate every dataset byte-identically.
numpy==2.5.1
pillow==12.3.0
scipy==1.18.0

# Solutions 2 & 3 (DriftMatchNet / DriftRoute -- the learned matcher).
# A CUDA build is recommended for speed; a CPU build also works (slower).
# The classical path needs no torch, and the router (predict_router.py) falls
# back to the classical path automatically when torch or a GPU is unavailable,
# so the inference script always runs.
torch>=2.4
